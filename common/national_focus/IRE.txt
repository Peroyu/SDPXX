focus_tree = {
	id = IRE_new
	country = {
		factor = 0
		modifier = {
			add = 10
		    tag = IRE  
		}	
	}
	default = no
    shared_focus = IRE_1
}
focus_tree = {
	id = IRE_lefttree
	country = {
		factor = 0
		modifier = {
			add = 10
		    tag = IRE  
		}	
	}
	default = no
    shared_focus = IRE_6
}

