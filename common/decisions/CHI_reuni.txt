CHI_reuni = {
	CHI_attak_menggu = {
		icon = decision_generic_prepare_civil_war
		fire_only_once = yes
		allowed = {
			original_tag = MMA
		}
		available = {
			tag = MMA
			check_variable = { #检查变量大小
                    var = global.chi_uni_nuon
                    value = 5
                    compare = greater_than_or_equals
            }
		}
		visible = {
			tag = MMA
			RCR = {
				has_war = yes
			}
		}
		cost = 0
		complete_effect = {
			declare_war_on = { target = MEN type = puppet_wargoal_focus }
			hidden_effect = {
				MEN = {
					add_ideas = CHI_WAR_TENSION_condition
				}
			}
			if = {
				limit = {
					JIN = {
						has_war = yes
					}
				}
				add_to_variable = { var = global.chi_uni_nuon value = 1 }
				custom_effect_tooltip = CHI_war_added
			}
			else_if = {
				limit = {
					NOT = {
						JIN = {
							has_war = yes
						}
					}
				}
				JIN = {
					country_event = SHA.1
					days = 1
				}
			}
		}
	}
	CHI_enter_china = {
		icon = decision_generic_prepare_civil_war
		fire_only_once = yes
		allowed = {
			original_tag = JAP
		}
		available = {
			tag = JAP
			check_variable = { #检查变量大小
                    var = global.chi_uni_nuon
                    value = 7
                    compare = greater_than_or_equals
            }
		}
		visible = {
			tag = JAP
			RCR = {
				has_war = yes
			}
		}
		cost = 0
		complete_effect = {
			declare_war_on = { target = CJF type = puppet_wargoal_focus }
			add_to_variable = { var = global.chi_uni_nuon value = 1 }
			custom_effect_tooltip = CHI_war_added
			add_ideas = CHI_WAR_TENSION_condition
		}
	}
	CHI_call_manchu = {
		icon = decision_generic_prepare_civil_war
		fire_only_once = yes
		allowed = {
			original_tag = RCR
		}
		available = {
			tag = RCR
			check_variable = { #检查变量大小
                    var = global.chi_uni_nuon
                    value = 6
                    compare = greater_than_or_equals
            }
			OR = {
			NOT = {
					RCR = {
						controls_state = 908
					}
					RCR = {
						controls_state = 596
					}
				
			}
		}
		}
		visible = {
			tag = RCR
			RCR = {
				has_war = yes
			}
		}
		cost = 0
		complete_effect = {
			MAN = {
				declare_war_on = { target = CJF type = puppet_wargoal_focus }
				add_to_variable = { var = global.chi_uni_nuon value = 1 }
				custom_effect_tooltip = CHI_war_added
				add_ideas = CHI_WAR_TENSION_condition
			}
		}
	}
	CHI_call_huabei = {
		icon = decision_generic_prepare_civil_war
		fire_only_once = yes
		allowed = {
			original_tag = RCR
		}
		available = {
			tag = RCR
			check_variable = { #检查变量大小
                    var = global.chi_uni_nuon
                    value = 5
                    compare = greater_than_or_equals
            }
			
			NOT = {
				RCR = {
					controls_state = 600
				}
				OR = {
					RCR = {
						controls_state = 602
					}
					HNN = {
						controls_state = 602
					}
				}
			}
		}
		visible = {
			tag = RCR
			RCR = {
				has_war = yes
			}
		}
		cost = 0
		complete_effect = {
			FNC = {
				declare_war_on = { target = CJF type = puppet_wargoal_focus }
				add_to_variable = { var = global.chi_uni_nuon value = 1 }
				custom_effect_tooltip = CHI_war_added
				add_ideas = CHI_WAR_TENSION_condition
			}
			if = {
				limit = {
					check_variable = { #检查变量大小
            			var = global.CHI_huabeifankang
           			 	value = 50
            			compare = greater_than_or_equals
					}
					check_variable = { #检查变量大小
            			var = global.CHI_huabeifankang
           			 	value = 100
            			compare = less_than
					}
				}
				FNC = {
					country_event = {
						id = FNC.1
						days = 1
					}
				}
			}
			else_if = {
				limit = {
					check_variable = { #检查变量大小
            			var = global.CHI_huabeifankang
           			 	value = 100
            			compare = greater_than_or_equals
					}
				}
				FNC = {
					country_event = {
						id = FNC.2
						days = 1
					}
				}
			}
		}
	}

}
