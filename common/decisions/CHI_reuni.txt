CHI_reuni = {
	RCR_reuni_1 = {
		icon = 
		fire_only_once = yes
		available = {
			FNC = {
				has_war = no
			}
			tag = RCR
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 4
				compare = greater_than_or_equals
			}
		}
		allowed = {
			original_tag = RCR
		}
		visible = {
			FNC = {
				has_war = no
			}
			tag = RCR
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 4
				compare = greater_than_or_equals
			}
		}
		cost = 10
		complete_effect = {
			add_to_variable = { global.chi_uni_nuon = 1 }
			custom_effect_tooltip = CHI_war_added
			FNC = {
				add_ideas = CHI_sourthwest_crisis
				declare_war_on = {target = CJF type = annex_everything }
			}
		}
		ai_will_do = {
			base = 100#ai点选这个决议的意愿
			}
	}
	RCR_reuni_2 = {
		icon = 
		fire_only_once = yes
		available = {
			MAN = {
				has_war = no
			}
			tag = RCR
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		allowed = {
			original_tag = RCR
		}
		visible = {
			MAN = {
				has_war = no
			}
			tag = RCR
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		cost = 10
		complete_effect = {
			add_to_variable = { global.chi_uni_nuon = 1 }
			custom_effect_tooltip = CHI_war_added
			MAN = {
				add_ideas = CHI_sourthwest_crisis
				declare_war_on = {target = CJF type = annex_everything }
			}
		}
		ai_will_do = {
			base = 100#ai点选这个决议的意愿
			}
	}
	RCR_reuni_3 = {
		icon = 
		fire_only_once = yes
		available = {
			MEN = {
				has_war = no
			}
			tag = MMA
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		allowed = {
			original_tag = MMA
		}
		visible = {
			MEN = {
				has_war = no
			}
			tag = MMA
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		cost = 10
		complete_effect = {
			add_to_variable = { global.chi_uni_nuon = 1 }
			custom_effect_tooltip = CHI_war_added
			MMA = {
				declare_war_on = {target = MEN type = annex_everything }
				
			}
			MEN = {
				add_ideas = CHI_sourthwest_crisis
			}
			MAN = {
				add_ideas = CHI_sourthwest_crisis
				declare_war_on = {target = MMA type = annex_everything }
			}

		}
		ai_will_do = {
			base = 100#ai点选这个决议的意愿
			}
	}
	RCR_reuni_4 = {
		icon = 
		fire_only_once = yes
		available = {
			JIN = {
				has_war = no
			}
			tag = MMA
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		allowed = {
			original_tag = MMA
		}
		visible = {
			JIN = {
				has_war = no
			}
			tag = MMA
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		cost = 10
		complete_effect = {
			add_to_variable = { global.chi_uni_nuon = 1 }
			custom_effect_tooltip = CHI_war_added
			JIN = {
				country_event = {
					id = SHA.1
					days = 1
				}
			}
		}
		ai_will_do = {
			base = 100#ai点选这个决议的意愿
			}
	}
	RCR_reuni_5 = {
		icon = 
		fire_only_once = yes
		available = {
			NIP = {
				has_war = no
			}
			tag = RCR
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 7
				compare = greater_than_or_equals
			}
		}
		allowed = {
			original_tag = RCR
		}
		visible = {
			NIP = {
				has_war = no
			}
			tag = RCR
			has_global_flag = ASIA_CHI_JAP_WAR
			check_variable = {
				var = global.chi_uni_nuon
				value = 6
				compare = greater_than_or_equals
			}
		}
		cost = 10
		complete_effect = {
			add_to_variable = { global.chi_uni_nuon = 1 }
			custom_effect_tooltip = CHI_war_added
			NIP = {
				add_ideas = CHI_sourthwest_crisis
				declare_war_on = {target = CJF type = annex_everything }
			}
		}
		ai_will_do = {
			base = 100#ai点选这个决议的意愿
			}
	}

}
