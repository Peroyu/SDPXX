add_namespace = GNPintro
add_namespace = GNP
country_event = {
    id = GNPintro.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event
	GNPintro.1 FROM = [From.GetName]" }
	title = GNPintro.1.t
	desc = GNPintro.1.d
	picture = GFX_GNP
	
	is_triggered_only = yes
	
	immediate = {
	    
    }
	
	option = {
		declare_war_on = { 
		target = GDC
		type = take_state_focus 
		}
	    ai_chance = {
		   base = 100
		}
		option = { 
		name = GNPintro.1.a
		}
	}
	
}