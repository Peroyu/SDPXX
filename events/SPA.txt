add_namespace = SPA
news_event = {
    id = SPA.1#开战
	immediate = {log = "[GetDateText]: [Root.GetName]: event
	SPA. FROM = [From.GetName]" }
	title = SPA.1.t
	desc = SPA.1.d
	picture = GFX_SPA_1
	
	is_triggered_only = yes
	
	immediate = {
	    hidden_effect = {
		
		}
    }
	
	option = { 
	    ai_chance = {
		   base = 100
		}
		
		name = SPA.1.a
	}
	
}